name = "link-memo-backup-worker"
main = "src/index.js"
compatibility_date = "2026-07-15"

[[r2_buckets]]
binding = "BACKUPS"
bucket_name = "link-memo-backups"

[vars]
# Set FIREBASE_PROJECT_ID and ALLOWED_ORIGINS in the Cloudflare dashboard or with wrangler secret.
# Use the deployed application origin and explicit local development origins.
